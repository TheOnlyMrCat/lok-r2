run 0 () >=> {
    a: bit = 1;
    b := 32;

    if (a) a = 0;

    if (b <= 42) {
        c := 20;
        if (c != 20) b = 4;
        else b = 20; // This syntax will be changed, as whitespace is necessary between "else" and "b"
    } else {
        b = 42;
    }
}

load(std:io)

run 1 () >=> {
    /* Declaration, Expression, Expression */
    for (i := 0; i <= 10; i++) {
        std.out.println(i);
    }
}